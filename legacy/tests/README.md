# Quarantined Tests

This directory stores tests and test artifacts that remain useful for historical inspection but are no longer part of the active runtime contract floor.

The active floor lives under `tests/` and should stay small:

- import smoke
- TF/USM/soft-OMP smoke
- one real-data integration path
