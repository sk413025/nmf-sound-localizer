Tools: Update inspect_flow.py for Freq-Aware Policy Verification

Experiment context:
- Background: We successfully set up the Frequency-Aware Policy (`SeqDT_FreqAware`). To evaluate its true physical effectiveness, we need to measure "Energy Reduction", not just "Imitation Accuracy".
- Motivation: The existing `inspect_flow.py` was designed for the old Global/Blind model and lacked the `freq_idx` input support required by the new architecture.
- Purpose: Upgrade `inspect_flow.py` to support `SeqDT_FreqAware`, enabling interactive "Environment-in-the-loop" rollouts to calculate actual reduction.

Implementation details:
- Imported `SeqDT_FreqAware` instead of `SeqDT_RTG`.
- Updated Model Init to include `max_freq=1025`.
- Updated `dt_input` construction to pass `freq_idx` (scalar/tensor) to the model.
- Added logic to track Frequency Bin Index (`b_idx`) and feed it as context.

Verification (Smoke Test):
- Model: `results/smoke_train_freq_aware/dt_freq_aware_best.pth` (2 epochs).
- Data: `results/smoke_data/lag_trajectories.pt` (Speech 260, Bin 50).
- Execution: `python3 scripts/h_exploration/inspect_flow.py ...`
- Results:
    - Step 0: DTmin Reduction 66.61% (Matches OMP 66.61%). ✅
    - Step 1: DTmin Reduction 66.61% (Stalled vs OMP 74.08%).
- Conclusion: The tool correctly executes the policy in the environment. The "Stall" is expected for a smoke-test model (undertrained). The Step 0 match confirms the model uses the Frequency Embedding to resolve phase correctly for the first step.

Next Steps:
- Perform full-scale training on the complete dataset.
- Use this tool to monitor "Final Reduction" alongside "Validation Accuracy".
