# Package-Era Contributing Note

This repository previously used a generic package-development contribution model. That model is no longer the primary operating guide for the `paper/nature-comm` worktree.

If you need package-era context:

- inspect older versions of `CONTRIBUTING.md` in git history
- inspect `nmf_localizer/README.md`
- use historical context only as reference, not as the source of truth for current branch governance
