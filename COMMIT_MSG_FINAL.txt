Setup: Interspeech GRU1 Experiment Plan (boy1 data + Physics-Based MDP)

Detailed setup for reproducing the 'Full Spectrum K=8' experiment (b482ddf)
using the 'boy1' subject dataset and the terminologies from the manuscript branch (c183fd4).

1. Data Registry & Preprocessing Requirements
   - Source Root: /Users/jnrle/Documents/LDVReorientation/data/SpeechData/boy1
   - Input Structure: Flat MIC/*.wav and LDV/*.wav folders.
   - Target Structure: Must transform to 'angle_{deg}/clip.npy' format for compatibility.
   - Action Item: Duplicate normalization logic from 'normalize_datasets.py' but adapted for raw WAV parsing.

2. Terminology Mapping (Manuscript Alignment c183fd4)
   - OMP -> Physics-motivated ℓ0 objective (Greedy Energy Capture).
   - Decision Transformer -> MDP Agent (Frequency-Aware Policy).
   - Return-to-Go (RTG) -> Physics-meaningful Energy Target.
   - Lag Selection -> Phase Alignment Action.
   - Correlation Profile -> Correlation State c_k(f).

3. Experiment Replication Logic (b482ddf)
   - Oracle Generation: generate_lag_omp.py (Teacher). Creates state-action pairs maximizing energy capture.
   - Student Training: train_dt_lag_seq_rtg.py.
     - Spectrum: Full (Bins 5-1024).
     - Budget: K=8 steps (found optimal for Reverb Tail in previous exp).
     - Epochs: 100.
   - Objective: Validate that the single 'Frequency-Aware Policy' can generalize to a new physical subject ('boy1').

This commit establishes the docs/experiment_plans/interspeech_gru1_plan.md acting as the source of truth.
