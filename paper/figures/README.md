# Figures

Place final, manuscript-ready figure assets in this folder (stable filenames).

Guidelines:
- Keep intermediate/generated artifacts under `results/<run_name>/`.
- Copy only final submission assets here with stable, descriptive, sortable names (e.g., `fig01_paradigm-shift.pdf`, `fig05_routing-mechanism-analysis.tiff`).
